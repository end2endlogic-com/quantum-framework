# Conformance vectors (copied)

Source of truth: helixor-ontologies/vectors/*.json (HelixorAI/helixor-ontologies).
These pin cross-stack ontology semantics and the canonical TBox hash
(helixor-ontologies/docs/HASH_SPEC.md). When the source vectors change, re-copy
them here; VectorConformanceTest and CanonicalTBoxHasherTest must pass against
the same files the Python suite uses.
